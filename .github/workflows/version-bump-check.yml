name: version-bump-check

on:
  pull_request:

permissions:
  contents: read

jobs:
  enforce-version-bump:
    uses: your-org/github_pipeline_governance/.github/workflows/version-bump-check.yml@v1.0.0
    with:
      version_file: VERSION
