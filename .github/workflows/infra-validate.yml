name: infra-validate

on:
  pull_request:

permissions:
  contents: read

jobs:
  terraform-validate:
    uses: your-org/github_pipeline_governance/.github/workflows/infra-validate.yml@v1.0.0
    with:
      config_directory: ./infra
